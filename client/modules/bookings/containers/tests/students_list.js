const {describe, it} = global;
import {expect} from 'chai';
import {stub, spy} from 'sinon';
import {composer, depsMapper} from '../students_list';

describe('bookings.containers.students_list', () => {
  describe('composer', () => {

  });
  describe('depsMapper', () => {

  });
});
